<%= form_with(model: cat_meme, local: true) do |form| %>
  <% if cat_meme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cat_meme.errors.count, "error") %> prohibited this cat_meme from being saved:</h2>

      <ul>
        <% cat_meme.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :alt %>
    <%= form.text_field :alt %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
